name: vocotest
description: "Voco Test Uygulaması"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.2.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.9
  ndialog: ^4.3.1
  shared_preferences: ^2.2.2
  internet_connection_checker: ^1.0.0+1
  cupertino_icons: ^1.0.6
  dio: ^5.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.1


flutter:

  uses-material-design: true

